from django.urls import path
from . import views

app_name = "accounts"

urlpatterns = [
    # 필요한 추가 API 엔드포인트를 이곳에 정의
    # 예시: path('profile/', views.UserProfileView.as_view(), name='user-profile'),
]
